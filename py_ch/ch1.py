#print all elements of a list in one line

mylist=[20, "years", "ago"]

for e in (mylist):
    #At the end of the output of the print statement in python. By default, the print function ends with a newline. Passing the whitespace to the end parameter (end=' ') indicates that the end character has to be identified by whitespace and not a newline.
    print(e, end=" ")

